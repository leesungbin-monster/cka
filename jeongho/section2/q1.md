## Configmap 수정하기

---

### Solution

```
# 1. pod 확인
k get pods -n nginx-static

# 2. Configmap 확인
k get cm -n nginx-static

# 3. Configmap 수정
k edit cm -n nginx-static nginx-config

 -> ssl_protocols TLSv1.3 TLSv1.2; #TLSv1.2 추가

# 4. pod 삭제로 재기동
k delete pods nginx-static-65c67d994c-lx2qb -n nginx-static

# 5. 재기동 확인
k get pods -n nginx-static
```

### Result

```
[cka0001@k8s-master ~]$ curl -k --tls-max 1.2 https://web.k8s.local:30007
OK
```